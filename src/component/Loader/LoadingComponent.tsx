'use client';
import { Spinner } from 'flowbite-react'; // Importing Spinner component

export default function LoadingComponent() {
  return <Spinner aria-label="Default status example" />; // Returning the Spinner component
}
